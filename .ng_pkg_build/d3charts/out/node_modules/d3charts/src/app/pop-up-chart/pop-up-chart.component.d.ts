import { OnInit, ViewContainerRef, ComponentFactoryResolver } from '@angular/core';
import { StandardBuilder } from '../DataBuilders';
import { DisplayComponent } from '../dynamic-display/IDisplayComponent';
export declare class PopUpChartComponent extends DisplayComponent<StandardBuilder> implements OnInit {
    private resolver;
    private selectedDirective;
    lineChart: any;
    barChart: any;
    isLineChart: Boolean;
    constructor(resolver: ComponentFactoryResolver, selectedDirective: ViewContainerRef);
    ngOnInit(): void;
    loadComponents(): void;
    showLineChart(): void;
    protected postLoad(): void;
}
