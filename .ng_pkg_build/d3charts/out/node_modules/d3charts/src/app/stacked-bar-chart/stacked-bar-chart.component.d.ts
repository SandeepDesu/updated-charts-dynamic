import { OnInit } from '@angular/core';
import { DisplayComponent } from '../dynamic-display/IDisplayComponent';
import { GroupedBuilder } from '../DataBuilders';
export interface Margin {
    top: number;
    right: number;
    bottom: number;
    left: number;
}
export declare class StackedBarChartComponent extends DisplayComponent<GroupedBuilder> implements OnInit {
    private margin;
    private width;
    private height;
    private svg;
    private x;
    private y;
    private z;
    private g;
    constructor();
    protected postLoad(): void;
    private initMargins();
    private initSvg();
    private drawChart();
}
