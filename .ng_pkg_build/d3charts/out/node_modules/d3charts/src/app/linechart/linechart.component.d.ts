import { OnInit } from '@angular/core';
import { DisplayComponent } from '../dynamic-display/IDisplayComponent';
import { StandardBuilder } from '../DataBuilders';
export declare class LinechartComponent extends DisplayComponent<StandardBuilder> implements OnInit {
    private margin;
    private width;
    private height;
    private x;
    private y;
    private svg;
    private line;
    constructor();
    protected postLoad(): void;
    private initSvg();
    private initAxis();
    private drawAxis();
    private drawLine();
}
