import { OnInit } from '@angular/core';
import { DisplayComponent } from '../dynamic-display/IDisplayComponent';
import { StandardBuilder } from '../DataBuilders';
export declare class BarChartComponent extends DisplayComponent<StandardBuilder> implements OnInit {
    private width;
    private height;
    private margin;
    private x;
    private y;
    private svg;
    private g;
    constructor();
    protected postLoad(): void;
    private initSvg();
    private initAxis();
    private drawAxis();
    private drawBars();
}
